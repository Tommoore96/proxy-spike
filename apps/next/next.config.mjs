/** @type {import('next').NextConfig} */
const nextConfig = {
  // async rewrites() {
  //   return [{ source: "/test", destination: "http://localhost:5001/" }];
  // },
};

export default nextConfig;
